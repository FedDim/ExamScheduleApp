<Window x:Class="ExamScheduleApp.EditGroupsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование групп" Height="400" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="GroupsDataGrid" 
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Grid.Row="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название группы" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
                <DataGridTemplateColumn Header="Действия" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Удалить" Click="DeleteGroup_Click" 
                                    Background="Red" Foreground="White" Margin="2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Сохранить изменения" Click="SaveChanges_Click" 
                    Background="Green" Foreground="White" Padding="10,5" Margin="0,0,10,0"/>
            <Button Content="Закрыть" Click="Close_Click" Padding="10,5"/>
        </StackPanel>
    </Grid>
</Window>